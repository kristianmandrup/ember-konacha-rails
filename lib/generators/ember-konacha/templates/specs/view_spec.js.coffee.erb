#= require spec_helper

describe "<%= view_name %>", ->
  beforeEach( ->
    # create view
    # Test.view = <%= view_name %>.create

    # Pass in a controller somehow?
  )

  it "is there", ->
    assert.ok <%= view_name %>

  it "displays something", ->
    # assert.equal Test.view.content
