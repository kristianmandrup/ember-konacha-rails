#= require spec_helper

describe "App.<%= class_name %>Controller", ->
  beforeEach( ->
    Test.store = Util.lookupStore()

    Ember.run( ->
      Test.store.loadOne(<%= model_name %>, id: 1, name: 'Alpha')

      controller = <%= controller_name %>.create()
      controller.set 'content', store.findOne(<%= model_name %>, 1)
    )
  )

  it "displays items", ->
    assert.equal  controller.content
    assert.equal  controller.content.get('id'), '1'
