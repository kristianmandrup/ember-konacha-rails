// #= require spec_helper

describe("<%= view_name %>", function() {
  beforeEach(function() {
    # create view
    # Test.view = <%= view_name %>.create();

    # Pass in a controller
  });

  it("is there", function() {
    assert.ok(<%= view_name %>);
  });

  it("displays something", function() {
    # assert.equal Test.view.content
  });
});